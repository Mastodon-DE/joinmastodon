# JoinMastodon


How to deploy
First install pnpm
then...

```
pnpm install
pnpm run dev
```


How to update on server

`sudo docker compose pull`
`sudo docker compose down`
`sudo docker compose up -d`
